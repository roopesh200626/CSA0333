#include <stdio.h>

void fibonacciSeries() {
    int n = 10;
    int a = 0, b = 1, next;
    int i;

    printf("Fibonacci Series up to %d terms:\n", n);
    for (i = 1; i <= n; i++) {
        printf("%d ", a);
        next = a + b;
        a = b;
        b = next;
    }
    printf("\n");
}

int main() {
    fibonacciSeries();
    return 0;
}
